export default interface Queue<T> {
}
export default class Queue<T> {
    private _tasks;
    private __new_items;
    constructor(list?: any[]);
    put(item: T): void;
    get(): Promise<T>;
    empty(): boolean;
    close(): void;
}
